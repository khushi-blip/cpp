#include <stdio.h>
int main()
{
  int a[10] = {10, 32, 13, 42};
  for (int i =4-1; i >=0; i--) // to remove the 0's u can only access the written elements lke size of array is 4 so 4-1
  {
    printf("%d\t",a[i]);

  
  }

}